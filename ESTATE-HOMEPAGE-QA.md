# Direct estate homepage: local review

Estate navigation now opens below the restored warm-white Alex Nava heading and introduction. Three destinations open simple cotton-paper dialogs. Close, Escape and backdrop dismissal return directly to the selected landmark. Previous scene source and assets are retained; homepage boot does not import or initialize them. Production and social/fallback images are unchanged.

Accepted artwork retains its compositions. The generated technical mask exec-b92feee0-c0d4-4be6-aeda-400c1a07b7dc.png in the existing generated-images folder supplies a gently irregular alpha edge. Threshold180, 4px inset, 0.45px antialias blur; exports 960x640 and600x900, WebP quality38 method6. Desktop99920 bytes + portrait93690 bytes =193610 bytes. Alpha affects only artwork, not labels or focus outlines.

Browser checks: desktop,390x844 DPR2,844x390 landscape,200% CSS zoom. All categories open; keyboard focus wraps and restores. Rapid close/reopen stays visible after the prior timeout. Backdrop dismissal returns directly. Reduced motion reports zero animation. Forced colors retains system controls. Blocked estate requests leave usable labels. JavaScript-disabled reload exposes anchors and current copy; inactive dialog buttons stay hidden through external CSS without an inline CSP exception.

Network inspection with legacy view=tower&angle=1&tour=5 parameters: stylesheet, fonts, small app bundle, manifest, estate artwork, and site icons only. No scene bundle, models or scene textures. Full suite285 passing; build and bundle verification pass. Estate direction visually accepted; release verification is recorded in the pull request.

Release preparation: production smoke checks cover estate app/CSS parity and reject scene boot; rollback still accepts prior scene releases. All 285 tests, compile verification, build/bundle checks, and dependency audit pass (zero vulnerabilities). GitHub Lighthouse and protected preview checks remain required before merging.
